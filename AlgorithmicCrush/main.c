#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
  unsigned int n;
  unsigned long long m;

  scanf("%u %llu", &n, &m);

  for (unsigned long long i = 0; i < m; i++) {
    unsigned int a, b, k;

    scanf("%u %u %u", &a, &b, &k);
  }

  return 0;
}
